<h2>Reservation New</h2>

<%= bootstrap_form_for @reservation do |r| %>
  <%= r.hidden_field :property_id, :value => @property %>
  <%= r.hidden_field :user_id, :value => @current_user.id %>
  <%= r.datetime_select :checkin %>
  <%= r.datetime_select :checkout %>

  <%= r.primary %>
<% end %>